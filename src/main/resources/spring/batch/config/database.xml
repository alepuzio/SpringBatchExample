<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
		http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/jdbc 
		http://www.springframework.org/schema/jdbc/spring-jdbc-4.3.xsd">

	<!-- connect to database -->
	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="org.apache.derby.jdbc.ClientDriver" />
		<property name="url"
			value="jdbc:derby://localhost:1527/spring_batch;" /><!-- create=true -->
		<property name="username" value="test" />
		<property name="password" value="test123_" />
	</bean>

<!-- il nome dell'ID e' statico perche' richiamato da altri oggetti Spring
per modificare la classe in base all'ambiente ci vuole file di properties -->
	<bean id="transactionManager"		class="org.springframework.batch.support.transaction.ResourcelessTransactionManager" />
	
	<!-- 
	bean id="transactionManagerProd"		class="org.springframework.jdbc.datasource.DataSourceTransactionManager" /
	-->

	<jdbc:embedded-database id="dataSource" type="DERBY">
		<jdbc:script location="classpath:script/create-user-table.sql" />
		<jdbc:script location="classpath:script/create-db.sql" />
		<jdbc:script location="classpath:script/insert-data.sql" />
	</jdbc:embedded-database>
</beans>